-- ==========================================================================
-- 数据库清理脚本 - 删除若依框架自带的测试/无用菜单
-- 执行前请备份 sys_menu 表！
-- ==========================================================================

-- ==========================================
-- 1. 删除【系统监控】下的外链菜单（Sentinel/Nacos/Admin 控制台）
--    这些是开发时用的控制台外链，生产环境不需要暴露
-- ==========================================
-- 先删按钮权限子项
DELETE FROM sys_menu WHERE parent_id IN (111, 112, 113);
-- 再删菜单本身
DELETE FROM sys_menu WHERE menu_id IN (111, 112, 113);

-- ==========================================
-- 2. 删除【系统工具】下的开发工具菜单
--    表单构建(114)、代码生成(115)、系统接口(116)
--    这些是 RuoYi 代码生成器工具，成品系统不需要
-- ==========================================
-- 先删代码生成的按钮权限 (1055-1060)
DELETE FROM sys_menu WHERE parent_id = 115;
-- 删菜单本身
DELETE FROM sys_menu WHERE menu_id IN (114, 115, 116);
-- 删整个系统工具目录（如果下面已空）
DELETE FROM sys_menu WHERE menu_id = 3 AND NOT EXISTS (SELECT 1 FROM (SELECT * FROM sys_menu) t WHERE t.parent_id = 3);

-- ==========================================
-- 3. 删除定时任务菜单（如不使用）
--    定时任务(110) 及其按钮权限(1049-1054)
-- ==========================================
-- DELETE FROM sys_menu WHERE parent_id = 110;
-- DELETE FROM sys_menu WHERE menu_id = 110;

-- ==========================================
-- 4. 可选：隐藏系统监控目录（如果您仍需在线用户功能）
--    将 visible 设为 1 即可隐藏，不删除
-- ==========================================
-- UPDATE sys_menu SET visible = '1' WHERE menu_id = 2;

-- ==========================================
-- 5. 清理 sys_dict_data 中的无用字典
--    若依自带的 sys_show_hide, sys_normal_disable 等保留
--    以下是开发时可能不需要的
-- ==========================================
-- （通常不建议删除字典数据，除非你确认没有使用）

-- ==========================================
-- 6. sys_dept 部门数据建议
--    保留根部门和你实际使用的学院部门
--    删除若依自带的测试子部门
-- ==========================================
-- 查看当前部门结构：
-- SELECT * FROM sys_dept ORDER BY parent_id, order_num;
-- 删除示例（若依默认的子部门，如"若依科技"下的测试部门）：
-- DELETE FROM sys_dept WHERE dept_id NOT IN (100, 实际学院ID列表);

-- ==========================================
-- 总结：安全可直接执行的删除
-- ==========================================
-- 以上 1、2 部分可安全执行（外链和开发工具）
-- 3、4、5、6 部分需要根据您的实际使用情况决定
-- 执行前务必 SELECT 确认数据，先删子项再删父项
