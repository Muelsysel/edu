@startuml 图2-3 成果申报审核业务流程图
skinparam backgroundColor #FFFFFF
skinparam defaultFontColor #000000
skinparam activityFontColor #000000
skinparam activityBorderColor #000000

|教师|
start
:登录系统;
:进入成果管理;
:点击新建成果;
:填写标题、类型、等级;
:在富文本编辑器中编写内容;
:上传证明材料附件;
if (保存为草稿?) then (是)
  :成果状态为草稿(0);
  :可继续编辑;
  stop
else (否，直接提交)
  :成果状态设为审核中(2);
endif

|审核员|
:登录系统;
:进入审核管理;
:查看待审成果列表;
:点击成果查看详情;
:阅读成果内容与附件;
:填写审核意见;
if (审核结论?) then (通过)
  :更新成果状态为已通过(3);
  :插入审核记录;
  :自动推送至门户公示;
else (驳回)
  :更新成果状态为已驳回(4);
  :插入审核记录含驳回意见;
endif

|教师|
if (成果被驳回?) then (是)
  :查看审核驳回意见;
  :根据意见修改成果内容;
  :点击重新提交;
  :状态恢复为审核中(2);
  detach
else (通过)
  :不可再修改;
  stop
endif

@enduml
